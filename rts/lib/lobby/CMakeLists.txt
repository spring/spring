
INCLUDE_DIRECTORIES(BEFORE ../)

SET(lobbySources
		"RawTextMessage"
		"Connection"
	)

ADD_LIBRARY(lobby STATIC EXCLUDE_FROM_ALL ${lobbySources})
TARGET_LINK_LIBRARIES(lobby md5 streflop ${Boost_SYSTEM_LIBRARY} ${Boost_THREAD_LIBRARY})
IF (MINGW)
	TARGET_LINK_LIBRARIES(lobby ${WIN32_LIBRARIES})
ENDIF (MINGW)

ADD_SUBDIRECTORY(Test)

